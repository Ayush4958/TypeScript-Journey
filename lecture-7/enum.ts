const enum SeatChoice {
    Aisle = 10, // We assign it a specific value here so next value of it will be incremented by  1
    Middle = 15 , // We can also assign this one specifically , we can also assign it as a string but for that i have to give every one a string value
    Window , 
    Fourth = "fourth"
}

Bseat = SeatChoice.Aisle